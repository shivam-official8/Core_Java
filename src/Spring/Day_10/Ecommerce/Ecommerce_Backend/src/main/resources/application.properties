spring.application.name=Ecommerce

#spring.datasource.url=jdbc:h2:mem:shivam

# postgres
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.defer-datasource-initialization=true
spring.datasource.url=jdbc:postgresql://localhost:5432/spring_boot
spring.datasource.username=postgres
spring.datasource.password=Right@811
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=true

# if table is already there then it will update it instead of creating new one

